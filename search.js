function linerSearch(arr, target) {
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === target) return i
    }
    return -1;
}


function binarySearch(arr, val) {
    const middleIdx = Math.floor((arr.lenght - 1) / 2)
    console.log(middleId);
}

